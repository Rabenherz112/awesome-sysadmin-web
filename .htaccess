# Generated by Awesome Selfhosted Website Generator
# This file removes .html extensions from URLs

RewriteEngine On

# Remove .html extension from URLs
# If the request has a .html extension
RewriteCond %{THE_REQUEST} \.html
# Redirect to the same URL without .html extension
RewriteRule ^(.*)\.html$ /$1 [R=301,L]

# Add .html extension internally for file resolution
# If the request is not for a file or directory that exists
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
# And the .html version of the file exists
RewriteCond %{REQUEST_FILENAME}.html -f
# Serve the .html file
RewriteRule ^(.*)$ $1.html [L]